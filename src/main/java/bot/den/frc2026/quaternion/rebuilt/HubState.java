package bot.den.frc2026.quaternion.rebuilt;

public enum HubState {
    INACTIVE,
    ACTIVE;

    static class Controller {
        static void setup(RebuiltStateMachine stateMachine) {

        }
    }
}
